# Day32 Lesson 3 — run 与端口映射

```text
宿主机 curl :8032  ──映射──►  容器内 uvicorn :8000
         -p 8032:8000
```

| 侧 | 端口 | 谁听 |
|----|------|------|
| 左边 `8032` | 宿主机 | 你本机浏览器 / curl |
| 右边 `8000` | 容器内 | Dockerfile 里 CMD 的 `--port 8000` |

`EXPOSE 8000` 只是声明；**真正打通宿主机要靠 `-p`**。
