[project]
name = "day20-agent-error-handling"
version = "0.1.0"
description = "Week3 Day20 tenacity retry and fallback for agent tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.16",
    "langchain-ollama>=1.1.0",
    "langgraph>=0.4.0",
    "tenacity>=8.0.0",
]
